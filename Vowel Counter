<?php
$String = "kaks de one";

$total = 0;
$vowels = Array('a','e','i','o','u');

for ($i=0; $i<strlen($String); $i++)
{
    for ($j = 0; $j<5; $j++)
        if ($String[$i] == $vowels[$j])
        {
            $total++;
            break;
        }
}
echo $total;
